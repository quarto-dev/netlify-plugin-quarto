name: 'netlify-plugin-quarto'
inputs:
  - name: version
    description: quarto version to install
    default: latest
    required: false
  - name: cmd
    description: quarto command to run 
    default: "render"
    required: false
